# Mobile testing rules

**Canonical (binding) document:** [`docs/mobile-testing-rules.md`](../../docs/mobile-testing-rules.md) at the **repository root**.

This path under `lender-app/docs/` exists only for stable deep links from the app folder. **Edit policy only in the root file** so there is a single source of truth.
