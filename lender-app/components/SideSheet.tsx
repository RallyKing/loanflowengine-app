/**
 * Semantic alias for right-anchored record surfaces — same contract and scroll
 * model as `RecordInspectorShell` (single inner body scrollport).
 */
export { RecordInspectorShell as SideSheet } from "./RecordInspectorShell";
